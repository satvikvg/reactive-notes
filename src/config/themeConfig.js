import * as colors from "@material-ui/core/colors";

const themes = [];

themes["indigoAmber"] = {
  name: "Indigo | Amber",
  palette: {
    primary: colors.indigo,
    secondary: colors.amber,
    error: colors.red,
    type: "light",
    contrastThreshold: 3,
    tonalOffset: 0.2
  }
};

themes["bluePink"] = {
  name: "Blue | Pink",
  palette: {
    primary: colors.blue,
    secondary: colors.pink,
    error: colors.red,
    type: "light",
    contrastThreshold: 3,
    tonalOffset: 0.2
  }
};

themes["lightBlueYellow"] = {
  name: "Light Blue | Yellow",
  palette: {
    primary: colors.lightBlue,
    secondary: colors.yellow,
    error: colors.red,
    type: "light",
    contrastThreshold: 3,
    tonalOffset: 0.2
  }
};

themes["cayanAmber"] = {
  name: "Cayan | Amber",
  palette: {
    primary: colors.cyan,
    secondary: colors.amber,
    error: colors.red,
    type: "light",
    contrastThreshold: 3,
    tonalOffset: 0.2
  }
};

themes["tealAmber"] = {
  name: "Teal | Amber",
  palette: {
    primary: colors.teal,
    secondary: colors.amber,
    error: colors.red,
    type: "light",
    contrastThreshold: 3,
    tonalOffset: 0.2
  }
};

themes["limeRed"] = {
  name: "Lime | Red",
  palette: {
    primary: colors.lime,
    secondary: colors.red,
    error: colors.red,
    type: "light",
    contrastThreshold: 3,
    tonalOffset: 0.2
  }
};

themes["deepOrangeCayan"] = {
  name: "Deep Orange | Cayan",
  palette: {
    primary: colors.deepOrange,
    secondary: colors.cyan,
    error: colors.red,
    type: "light",
    contrastThreshold: 3,
    tonalOffset: 0.2
  }
};

themes["brownAmber"] = {
  name: "Brown | Amber",
  palette: {
    primary: colors.brown,
    secondary: colors.amber,
    error: colors.red,
    type: "light",
    contrastThreshold: 3,
    tonalOffset: 0.2
  }
};

themes["greyOrange"] = {
  name: "Grey | Orange",
  palette: {
    primary: colors.grey,
    secondary: colors.orange,
    error: colors.red,
    type: "light",
    contrastThreshold: 3,
    tonalOffset: 0.2
  }
};

themes["greyOrange"] = {
  name: "Blue Grey | Amber",
  palette: {
    primary: colors.blueGrey,
    secondary: colors.amber,
    error: colors.red,
    type: "light",
    contrastThreshold: 3,
    tonalOffset: 0.2
  }
};

export { themes };
